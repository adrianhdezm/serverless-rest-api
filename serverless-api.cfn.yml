AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: Just Another Serverless Rest API

Globals:
  Function:
    Runtime: nodejs8.10

Resources:
  ApiGateway:
    Type: AWS::Serverless::Api
    Properties:
      StageName: Prod
      EndpointConfiguration: REGIONAL
      Cors:
        AllowMethods: "'*'"
        AllowHeaders: "'*'"
        AllowOrigin: "'*'"

  GetBasePathFunction:
    Type: AWS::Serverless::Function
    Properties:
      MemorySize: 128
      Timeout: 3
      Handler: index.handleGetBasePath
      Events:
        GetBasePath:
          Type: Api
          Properties:
            RestApiId: !Ref ApiGateway
            Path: /
            Method: GET
            Auth:
              Authorizer: NONE

  MultipleObjectsFunction:
    Type: AWS::Serverless::Function
    Properties:
      MemorySize: 192
      Timeout: 3
      Handler: index.handleMultipleObjects
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref Store
      Environment:
        Variables:
          TABLE_NAME: !Ref Store
      Events:
        QueryObjects:
          Type: Api
          Properties:
            RestApiId: !Ref ApiGateway
            Path: /classes/{className}
            Method: GET
        CreateObjects:
          Type: Api
          Properties:
            RestApiId: !Ref ApiGateway
            Path: /classes/{className}
            Method: PATCH

  SingleObjectFunction:
    Type: AWS::Serverless::Function
    Properties:
      MemorySize: 128
      Timeout: 3
      Handler: index.handleSingleObject
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref Store
      Environment:
        Variables:
          TABLE_NAME: !Ref Store
      Events:
        CreateObject:
          Type: Api
          Properties:
            RestApiId: !Ref ApiGateway
            Path: /classes/{className}
            Method: POST
        ReadObject:
          Type: Api
          Properties:
            RestApiId: !Ref ApiGateway
            Path: /classes/{className}/{objectId}
            Method: GET
        UpdateObject:
          Type: Api
          Properties:
            RestApiId: !Ref ApiGateway
            Path: /classes/{className}/{objectId}
            Method: PUT
        DeleteObject:
          Type: Api
          Properties:
            RestApiId: !Ref ApiGateway
            Path: /classes/{className}/{objectId}
            Method: DELETE

  Store:
    Type: AWS::DynamoDB::Table
    Properties:
      KeySchema:
        - AttributeName: 'objectId'
          KeyType: 'HASH'
      AttributeDefinitions:
        - AttributeName: 'objectId'
          AttributeType: 'S'
      ProvisionedThroughput:
        ReadCapacityUnits: 1
        WriteCapacityUnits: 1

Outputs:
  ApiId:
    Description: 'API Gateway Id'
    Value: !Ref ApiGateway

  ApiUrl:
    Description: 'API Gateway endpoint URL for Prod stage'
    Value: !Sub 'https://${ApiGateway}.execute-api.${AWS::Region}.amazonaws.com/Prod/'
    
